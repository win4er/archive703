package utils

func FormatFileSize(size int64) string {
    // Твоя реализация (уже есть в коде)
}

// Другие вспомогательные функции:
// - ExtractCommandArgs (чтобы получить аргументы из сообщения)
// - GenerateFileName (чтобы не было коллизий)
// - EnsureDir (создать папку, если её нет)
